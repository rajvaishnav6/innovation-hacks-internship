# Task 1 is frontend-only and uses mock data, so no environment variables
# are required to run this project yet.
#
# This file is here so the structure is ready for later tasks -- once this
# dashboard is connected to the real API you'll build in Task 2, point it
# at your backend below.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
